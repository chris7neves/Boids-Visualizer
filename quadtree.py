class quadTree():
    def __init__(self, boundary, n):
        # boundary should be a rectangle registered around a center point
        # each section needs to have a capacity of n

    def insert(self, point):
        # if capacity is not full, add point
        # if capacity is full, subdivide quadrant

    def subdivide(self):
        # subdivides an existing quadrant into 4 more quadrants